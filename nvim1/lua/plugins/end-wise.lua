return   {
    "tpope/vim-endwise",
    lazy = false,  -- Ensures the plugin is always loaded
    config = function()
      -- No additional configuration needed, endwise works out of the box
    end
  }